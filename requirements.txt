# Direct runtime dependencies only.
# Avoid pinning Scrapy's transitive tree from a local environment export so
# patched resolver output can move forward without rewriting this file.
Scrapy>=2.14.2,<3.0
itemadapter>=0.13.1,<1.0
python-dotenv>=1.2.2,<2.0
requests>=2.32.4,<3.0
